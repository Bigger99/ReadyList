@if (IssueList is not null)
{
    @for (int i = 0; i < IssueList.Length; i++)
    {
        <IssueViewer Issue="IssueList[i]"/>

        if (IssueList.Length > i)
        {
            <br />
        }
    }  
}

@code {
    [Parameter]
    public Issue[]? IssueList { get; set; }
}
